import 'package:flutter/material.dart';

class CelenderSearch extends StatelessWidget {
  const CelenderSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}